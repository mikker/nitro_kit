<%= component_page do %>
  <%= title "Badges" %>

  <div class="space-y-4">
    <%= example(class: "flex-col") do %>
      <%= badge "Default" %>
      <%= badge(variant: :outline) { "Outline" } %>
      <% end %>

    <%= code_example %{
    <%= badge(color: :teal) { "My badge" } %\>
    } %>

    <pre><code>&lt;%= badge(color: :teal) { "My badge" } %&gt;</code></pre>
  </div>
<% end %>
